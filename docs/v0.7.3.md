# v0.7.3 — Core Structure and Validation

v0.7.3 is a source release. It does not publish a new package distribution or runner image; the reviewed `pyfixagent-runner:0.7.2` image remains the configured default.

## Verification

On 2026-07-25, the release source completed the full pytest collection:

```text
python -m pytest -q
230 passed, 6 skipped in 131.30s
```

The six skips are conditional environment or integration coverage. No test failed.

## Release scope

- Refactored the repair engine, benchmark runner, runtime configuration, container sandbox, and metric entry points into smaller orchestration units. AST architecture tests limit the protected entry points and their collaborators.
- Kept model and execution boundaries explicit: `qwen3.7-plus` is the configured default model, while the runner image remains at its separately reviewed v0.7.2 version.
- Added three multi-file benchmark fixtures with external holdouts, bringing the local curated manifest to 27 cases.
- Added runner lock/base-version validation and rootless Podman bind-mount UID preservation.

This test result verifies the repository's checked-in test suite. It is not a new real-model repair-rate qualification and does not replace the separately documented benchmark evidence.
